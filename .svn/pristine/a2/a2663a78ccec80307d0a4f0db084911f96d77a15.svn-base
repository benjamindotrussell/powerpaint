/**
 * 
 */
package gui;

import java.awt.Dimension;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import javax.swing.JPanel;

/**
 * class that implements the JPanel.
 * 
 * @author ben
 * @version 11/5/2015
 */
public class PaintPanel {
    /** A size for the JPanel. */
    private static final Dimension PANEL_SIZE = new Dimension(400, 200);
    /** Variable for the top panel so I can check the state of it later. */    
    private final JPanel myPanel = new JPanel();
    
    /**
     * 
     */
    public PaintPanel() {
               
    }
    /**
     * method to place an action listener on the pain panel.
     */
    public void paintArea() {
        myPanel.setSize(PANEL_SIZE);
        myPanel.addActionLIstenter(new ActionListener() {
            public void actionPerformed(final ActionEvent theEvent) {
                
            }
        });
    }
}
